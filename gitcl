     git init

     git config --global user.name "wheezy"
     git config user.name
     git config --global user.email "whezzy@debvm"
     git config user.email

     git status 
     git add scpt.sh 
    git status

    git commit -m "end commend sleep"
    git branch -M master
    git checkout
    git pull -v

	git remote -v
	git remote remove origin
	git remote set-url --add  origin git@github.com:alprofmahmoud/termux_repo.git
	git remote get-url --push origin
    git remote add origin git@github.com:alprofmahmoud/debpjc.git 	#command line important

    ssh-keygen										#generate key and add the key in github
    ssh -T git@github.com
    git push -u origin master
